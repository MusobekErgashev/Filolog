import Auth from '@/components/Auth'
import React from 'react'

export const dynamic = "force-dynamic"

const page = () => {
  return (
    <div>
        <Auth />
    </div>
  )
}

export default page